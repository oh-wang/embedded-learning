# 嵌入式学习仓库

这是主人的嵌入式学习目录：`roadmap.md`（公开路线）、`posts/<日期>.md`（公开博客稿）、`memory/`（私有学习记忆）、`scripts/sync.sh`（同步脚本）。公开仓库用于博客发布；私有备份仓库保存整个目录，防止数据丢失。

## 读取时机（重要）

**不要一进对话就读记忆文件。** 只有当主人明确表示「开始学习 / 继续嵌入式学习」或「同步今天进度」时才读取 `memory/`。主人只是普通提问、调试代码或在该目录下干别的活时，`memory/` 视为不存在。

完整操作流程由 `embedded-learning` skill 提供（`~/.codex/skills/embedded-learning/SKILL.md`），触发后按它执行。

## 硬性约束

- 绝不把 `memory/` 内容提交到公开博客仓库（不要用 `git add -f` 强加）；它会由同步脚本复制到私有全量备份仓库。
- 发布稿：第一人称、口语化，不写教科书，不含私人信息、学校/单位、未公开项目细节。
- 不编造今天没发生的学习内容，日期用本机当天真实日期。
