# 嵌入式学习仓库 · 工作规则

本仓库是主人的嵌入式学习进度库。你（AI）是它的读写工具，记忆以文件为准，不以对话为准。

## 目录结构

| 路径 | 内容 | 是否公开 |
|---|---|---|
| `roadmap.md` | 学习路线总纲 | 公开 |
| `posts/<日期>.md` | 当天的发布稿 | 公开 |
| `memory/progress.md` | 当前进度快照、下次从哪继续 | 私有备份 |
| `memory/log/<日期>.md` | 每日原始记录 | 私有备份 |
| `memory/mistakes.md` | 错题与易错点 | 私有备份 |
| `scripts/sync.sh` | 同步脚本（AI 不修改它，除非主人要求） | 公开 |

## 每次对话开始

1. 读 `memory/progress.md`。
2. 用**一到两句话**告诉主人：上次学到哪、今天建议继续什么。
3. 不要复述整个文件，不要输出文件内容原文。

> 如果 `memory/progress.md` 里是「还没开始」之类的占位内容，就简短打个招呼，问今天想学什么。

## 对话进行中

- 主人问问题时正常回答，不要主动写文件。
- 顺手记下主人暴露出的薄弱点、卡住的地方，同步时写进错题。

## 当主人说「同步今天进度」（或类似的结束语）

按顺序做完下面全部动作：

1. **写当日原始记录** → `memory/log/<今天日期>.md`，用 `templates/daily-log.md` 的格式，至少包含这两项：
   - `- 今天主题：` 一句话概括
   - `- 学了什么：` 分条列出具体知识点、动手内容、结论
   如果今天有卡壳或错误，在下面追加一节 `## 卡点`。
2. **更新错题本** → 有新的易错点就追加到 `memory/mistakes.md`（现象 → 原因 → 正解）。
3. **写发布稿** → `posts/<今天日期>.md`，用 `templates/daily-post.md` 的格式：
   - 第一人称、口语化，像在社交平台分享，**不要写成教科书**
   - 标题带 Day 序号（= `memory/log/` 里的文件数）
   - 结尾列 2–4 条「今天主要学了」的要点
   - **不得包含**私人信息、具体学校/单位、未公开的项目细节
4. **更新进度快照** → 改写 `memory/progress.md` 的「当前进度 / 未掌握 / 下一步」三节，把已经会了的从「未掌握」移除，把下一步写成具体可执行的一句话。
5. **执行同步** → 在仓库根目录运行：

   ```bash
   bash scripts/sync.sh
   ```

   脚本会自动提交、推送公开仓库、备份记忆仓库、更新进度 Issue。
   若脚本报错，把错误原文转述给主人，不要自己瞎改脚本绕过。

6. 汇报同步结果，三行以内：今天学了什么、累计第几天、公开链接。

## 硬性约束

- **绝不**把 `memory/` 下的内容提交到公开仓库（`.gitignore` 已挡，不要用 `-f` 强加）。
- **绝不**把发布稿写成技术文档或摘要式罗列。
- **绝不**编造今天没发生的学习内容。记录必须以本次对话实际内容为准。
- 日期一律用本机当天真实日期，不要猜。

## 主人手动做什么

- 把学习计划贴进 `roadmap.md`。
- 平时学习、提问；结束时说一句「同步今天进度」。
- 其余全自动。
